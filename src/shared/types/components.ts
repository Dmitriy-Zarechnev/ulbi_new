export enum APP_LINK_THEME_ENUM {
    PRIMARY = 'Primary',
    SECONDARY = 'Secondary',
    RED = 'Red',
}

export enum BUTTON_THEME_ENUM {
    CLEAR = 'Clear',
    CLEAR_INVERTED = 'ClearInverted',
    OUTLINE = 'Outline',
    BACKGROUND = 'Background',
    BACKGROUND_INVERTED = 'BackgroundInverted',
}

export enum BUTTON_SIZE_ENUM {
    M = 'SizeM',
    L = 'SizeL',
    XL = 'SizeXL',
}
